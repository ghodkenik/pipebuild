logger.info "this is gradle project"

plugins {
    id 'java-gradle-plugin'
}

apply plugin 'java'

sourceCompatibility = 1.8
targetCompatibility = 1.8

dependencies {
    testImplementation "junit:junit:4.12"
}

jar {
    archiveBaseName = 'first-gradle'
    archiveVersion =  '0.0.1'
}
